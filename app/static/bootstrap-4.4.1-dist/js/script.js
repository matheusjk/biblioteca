/*    window.setTimeout(function() {
        $(".alert").fadeTo(500, 0.05).slideUp(800,
        function(){
            $(this).remove();
        });
    }, 3000);
*/

$(".alert").alert()  // usando a funcionalidade do proprio JS do bootstrap 4
